
package Questao06;

/*
 * Interfaces: Uma interface não tem implementação só assinatura de como os
 * métodos deve ser escritos nas classes assim todos os métodos são abstratos e
 * não tem como instancia um método ou criar um construtor. 
 * Abstratas: Deve
 * conter pelo menos um método abstrato sem corpo que deve ser implementado
 * pelas classes que estendes a classes abstrata assim forçando que todas as
 * classes filhas tenha esse método.
 */
